---
phase: 01-domain-foundation
plan: "01"
subsystem: infra
tags: [python, fastapi, uv, pyproject, scaffold, pytest]

# Dependency graph
requires: []
provides:
  - uv-managed Python project with pinned dependency lock file
  - FastAPI app skeleton (main.py) with asynccontextmanager lifespan
  - FAILURE_CONFIG and DEMO_MODE constants (config.py)
  - Full app/ and tests/ directory tree with 18 __init__.py package markers
affects:
  - 01-02-PLAN.md (domain models land in app/lib/)
  - 01-03-PLAN.md (seed.py wired into main.py lifespan)
  - All subsequent plans (depend on uv env and package imports)

# Tech tracking
tech-stack:
  added:
    - fastapi==0.136.0
    - uvicorn[standard]==0.44.0
    - pydantic==2.13.2
    - pydantic-settings==2.13.1
    - passlib==1.7.4
    - bcrypt==5.0.0
    - itsdangerous==2.2.0
    - pytest==9.0.3
    - pytest-asyncio==1.3.0
    - ruff==0.15.11
    - mypy==1.20.1
    - httpx==0.28.1
    - playwright==1.58.0
    - uv (package manager, flat layout)
  patterns:
    - asynccontextmanager lifespan replaces deprecated @app.on_event
    - package=false in [tool.uv] for flat layout (no src/)
    - pythonpath=["."] in pytest config enables absolute app.lib.* imports
    - FAILURE_CONFIG read at call time (never import time) for live mutation
    - Plain Python dict in-memory stores (no ORM, no database)

key-files:
  created:
    - pyproject.toml
    - uv.lock
    - .gitignore
    - main.py
    - config.py
    - app/__init__.py
    - app/api/__init__.py
    - app/web/__init__.py
    - app/lib/__init__.py
    - app/lib/auth/__init__.py
    - app/lib/catalog/__init__.py
    - app/lib/cart/__init__.py
    - app/lib/orders/__init__.py
    - app/lib/seed/__init__.py
    - app/lib/mocks/__init__.py
    - app/lib/agent/__init__.py
    - app/lib/observability/__init__.py
    - app/lib/guardrails/__init__.py
    - app/lib/evals/__init__.py
    - tests/__init__.py
    - tests/unit/__init__.py
    - tests/integration/__init__.py
    - tests/e2e/__init__.py
  modified: []

key-decisions:
  - "uv flat layout (package=false) — prevents uv from treating app/ as installable distribution"
  - "No python-jose (CVE-2024-33664) — passlib+bcrypt+PyJWT stack only"
  - "asynccontextmanager lifespan — not deprecated @app.on_event"
  - "pythonpath=[.] in pytest — belt-and-suspenders for absolute app.lib.* imports"
  - "FAILURE_CONFIG read at call time — enables live root-token mutation (Phase 5)"
  - "No anthropic/langfuse in Phase 1 — deferred to Phase 4/5 per plan"

patterns-established:
  - "Flat layout: app/ at project root, no src/ wrapper"
  - "All packages have __init__.py — no namespace package ambiguity"
  - "config.py: SCREAMING_SNAKE_CASE module-level constants, no pydantic-settings yet"
  - "main.py: asynccontextmanager lifespan, routers added incrementally in later phases"

requirements-completed: []

# Metrics
duration: 8min
completed: "2026-04-19"
---

# Phase 1 Plan 01: Project Scaffold Summary

**uv flat-layout Python project with FastAPI lifespan skeleton, FAILURE_CONFIG constants, and 18 app/tests package markers — fully importable, pytest-collectable**

## Performance

- **Duration:** ~8 min
- **Started:** 2026-04-19T02:45:39Z
- **Completed:** 2026-04-19T02:53:38Z
- **Tasks:** 3 of 3
- **Files created:** 23 (pyproject.toml, uv.lock, .gitignore, main.py, config.py, 18 __init__.py)

## Accomplishments

- Created pyproject.toml with correct uv flat layout (`package = false`), all Phase 1 runtime and dev deps, pytest asyncio_mode=auto and pythonpath=["."] config
- Ran `uv sync` successfully — 42 packages resolved and installed to .venv (uv.lock generated)
- Created full app/ and tests/ directory tree (14 app packages + 4 test packages) with __init__.py markers — all subpackages importable via absolute `from app.lib.*` paths
- Created main.py FastAPI skeleton with asynccontextmanager lifespan (no-op, Plan 03 wires seed)
- Created config.py with FAILURE_CONFIG dict (4 failure keys) and DEMO_MODE flag

## Task Commits

Each task was committed atomically:

1. **Task 1: Create pyproject.toml and .gitignore** - `6d8c95e` (chore)
2. **Task 2: Create app/ and tests/ __init__.py markers** - `9053acd` (chore)
3. **Task 3: Create main.py and config.py** - `e3a6ec0` (feat)

## Files Created/Modified

- `pyproject.toml` - uv project config with runtime/dev deps, pytest and ruff settings
- `uv.lock` - locked dependency tree (42 packages, generated by uv sync)
- `.gitignore` - excludes __pycache__, .venv, .env, .DS_Store, IDE dirs
- `main.py` - FastAPI app with asynccontextmanager lifespan stub
- `config.py` - FAILURE_CONFIG dict and DEMO_MODE flag
- `app/__init__.py` through `app/lib/evals/__init__.py` - 14 app package markers
- `tests/__init__.py` through `tests/e2e/__init__.py` - 4 test package markers

## Decisions Made

- `[tool.uv] package = false` is mandatory for flat layout — without it, uv attempts to build app/ as a distribution package and fails
- `pythonpath = ["."]` in pytest config ensures `from app.lib.catalog.models import Product` resolves in all invocation modes
- `asyncio_mode = "auto"` removes need for `@pytest.mark.asyncio` on every async test
- No `python-jose` anywhere — explicitly forbidden per CLAUDE.md (CVE-2024-33664)
- No `anthropic`, `langfuse`, or `PyJWT` in Phase 1 deps — deferred to Phase 4/5

## Deviations from Plan

None — plan executed exactly as written.

Minor observation: `uv run pytest --collect-only` exits with code 5 (pytest's standard "no tests collected" exit code) rather than 0. This is expected behavior for an empty test suite and is NOT an error — no ModuleNotFoundError or import failure occurs. The plan's actual intent (import-error-free collection) is fully satisfied.

## Issues Encountered

None. `uv sync` resolved 42 packages without conflict. All imports succeed. pytest collects 0 tests with no errors.

## Threat Surface

The `.gitignore` excludes `.env` and `.env.local` at creation time, satisfying T-01-01-02 (Information Disclosure). No network endpoints, no user input, no cryptographic operations in this plan.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Python environment fully operational — `uv run python -c "import app"` succeeds
- All package directories exist for Plan 02 to add models.py and store.py files
- `main.py` lifespan ready for Plan 03's `seed()` wiring
- `config.py` FAILURE_CONFIG shape locked — Phase 3 mock adapters can import it immediately

---
*Phase: 01-domain-foundation*
*Completed: 2026-04-19*
