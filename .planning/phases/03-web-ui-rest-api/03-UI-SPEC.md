---
phase: 3
slug: web-ui-rest-api
status: approved
reviewed_at: 2026-04-19
shadcn_initialized: false
preset: none
created: 2026-04-19
---

# Phase 3 — UI Design Contract

> Visual and interaction contract for the Jinja2/Bootstrap 5 web UI.
> Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (Bootstrap 5 CDN) |
| Preset | not applicable |
| Component library | Bootstrap 5.3 via CDN — `<link>` in base.html |
| Icon library | Bootstrap Icons 1.11 via CDN (bi-cart, bi-bag, bi-person, etc.) |
| Font | System font stack (Bootstrap default: -apple-system, Segoe UI, Roboto, etc.) |

Source: D-05, D-06 (CONTEXT.md)

No build step. All CDN links injected in `base.html`. No custom CSS file in Phase 3.

---

## Spacing Scale

Bootstrap 5 uses a 4px base unit with spacing utilities 0–5 (0, 4, 8, 16, 24, 48px). Map to the 8-point scale as follows:

| Token | Bootstrap class(es) | Value | Usage |
|-------|---------------------|-------|-------|
| xs | `gap-1`, `p-1`, `m-1` | 4px | Icon gaps, inline badge padding |
| sm | `gap-2`, `p-2`, `m-2` | 8px | Tight element spacing, input group gaps |
| md | `gap-3`, `p-3`, `m-3` | 16px | Default card/form padding |
| lg | `gap-4`, `p-4`, `m-4` | 24px | Section padding, card body |
| xl | `gap-5`, `p-5`, `m-5` | 48px | Page-level top/bottom padding |

Exceptions:
- Touch targets for buttons: minimum 44px height — use `btn-lg` (Bootstrap sets ~48px height) wherever the primary action button appears (Add to Cart, Checkout, Login, Register)
- Navbar height: Bootstrap default ~56px — do not override

---

## Typography

Bootstrap 5 default type scale; no additional @font-face declarations.

Four sizes only — no exceptions.

| Role | Bootstrap class | Size | Weight | Line Height |
|------|----------------|------|--------|-------------|
| Body | (default) | 16px | 400 (regular) | 1.5 |
| Label / small | `small`, `fs-6` | 14px | 400 (regular) | 1.4 |
| Heading (section) | `h5` | 20px | 600 (semibold) | 1.2 |
| Display (page title) | `h2`, `display-6` | 28px | 600 (semibold) | 1.2 |

Weights in use: 400 (regular) and 600 (semibold) — no other weights.

Note: `h4` (24px) is removed from the scale. All in-page section headings use `h5` (20px). Page-level titles use `h2`/`display-6` (28px) only.

Source: Bootstrap 5 defaults (D-06: minimal customization)

---

## Color

Bootstrap 5 default palette — no custom brand colors in Phase 3.

| Role | Bootstrap token | Hex (Bootstrap default) | Usage |
|------|----------------|------------------------|-------|
| Dominant (60%) | `bg-white` / `bg-body` | #ffffff | Page background, form surfaces |
| Secondary (30%) | `bg-light` / `bg-body-tertiary` | #f8f9fa | Navbar background, product cards, sidebar area |
| Accent (10%) | `btn-primary` / `text-primary` / `bg-primary` | #0d6efd | See accent reserved list below |
| Destructive | `btn-danger` / `text-danger` | #dc3545 | Destructive actions only (Cancel Order, confirm modal) |

**Accent reserved for (explicit list — not all interactive elements):**
1. Primary CTA buttons: "Add to Cart", "Checkout", "Login", "Register", "Reset Password"
2. Active category filter tab on product list
3. "View Details" links on product cards and orders list
4. Order status badge for `shipped` status

**Status badge color mapping (Bootstrap badge classes):**

| Order Status | Bootstrap class |
|-------------|----------------|
| placed | `bg-secondary` |
| paid | `bg-info text-dark` |
| processing | `bg-warning text-dark` |
| shipped | `bg-primary` |
| canceled | `bg-danger` |

Source: D-06 (no custom brand colors), specifics section (colored status badge)

---

## Page Inventory

All pages in scope. Template file names are prescriptive.

| URL | Template | Auth required | Notes |
|-----|----------|--------------|-------|
| `/` | redirect to `/products` | no | home index redirects |
| `/products` | `templates/products/list.html` | no | keyword search + category tabs |
| `/products/{id}` | `templates/products/detail.html` | no | variant selector + Add to Cart form |
| `/cart` | `templates/cart/cart.html` | yes (redirect) | cart items + checkout form |
| `/orders` | `templates/orders/list.html` | yes (redirect) | orders list with status badges |
| `/orders/{id}` | `templates/orders/detail.html` | yes (redirect) | order detail + cancel/return buttons |
| `/orders/{id}/confirmation` | `templates/orders/confirmation.html` | yes (redirect) | post-checkout confirmation |
| `/login` | `templates/auth/login.html` | no | demo credential hint |
| `/register` | `templates/auth/register.html` | no | |
| `/auth/reset-request` | `templates/auth/reset_request.html` | no | |
| `/auth/reset-confirm` | `templates/auth/reset_confirm.html` | no | token from API response body |

Source: D-16 (CONTEXT.md)

---

## Component Inventory

Prescriptive Bootstrap components per page.

### base.html (all pages inherit)
- `navbar navbar-expand-lg bg-light navbar-light` with brand text "ShoeStore"
- Navbar links: Products (`/products`), Cart (`/cart` with item count badge), Orders (`/orders`), Login/Logout
- Flash message zone below navbar: Bootstrap `alert alert-{type} alert-dismissible` blocks; rendered from Jinja2 session flash
- `container` wrapping block content (max-width responsive)

### Product List (`/products`)
**Focal point:** The primary visual anchor is the product card grid; the "View Details" `btn-primary` stretched-link on each card is the sole conversion action per product.

- Search: `input-group` with text input and "Search" `btn-primary` button, `GET /products?q=`
- Category tabs: `nav nav-tabs` with links: All / Running / Hiking / Slides / Sandals / Socks. Active tab has `active` class. Combines with `?q=` param.
- Products grid: `row row-cols-1 row-cols-md-3 g-4` of `card h-100`
- Each card: `card-img-top` placeholder (gray `bg-secondary` div, 200px height), `card-body` with `card-title` (name), `card-text small` (category), price `fw-semibold`, "View Details" `btn btn-primary btn-sm stretched-link`
- No inline Add to Cart on the list page

### Product Detail (`/products/{id}`)
**Focal point:** The primary visual anchor is the full-width "Add to Cart" `btn btn-primary btn-lg w-100` button — variant selectors and quantity are prerequisites that lead the eye directly to it.

- `row` with col-md-6 left (product image placeholder) and col-md-6 right (info + form)
- Info: `h2` name, `text-muted` category, price `fs-3 fw-semibold`, inventory `small text-muted`
- Variant selector: `<select class="form-select">` for Size and `<select class="form-select">` for Color
- Quantity: `<input type="number" class="form-control" min="1" value="1">` default 1
- "Add to Cart" `btn btn-primary btn-lg w-100` — POST form to `/cart/add`
- On success: redirect to `/cart`

### Cart (`/cart`)
**Focal point:** The primary visual anchor is the "Place Order" `btn btn-primary btn-lg w-100` button in the order summary card — the cart table and payment selector exist solely to lead to this action.

- Cart items: `table table-borderless` with columns: Product, Size/Color, Qty (editable input), Unit Price, Subtotal, Remove (X button)
- Order summary card: `card` right column with subtotal, "Select Payment Method" section
- Payment method: `form-check` radio buttons — Credit Card, PayPal, Apple Pay (one selected by default)
- "Checkout" `btn btn-primary btn-lg w-100` — POST to `/checkout`
- Empty cart state: centered `text-muted` with message and "Browse Products" link

### Orders List (`/orders`)
**Focal point:** The primary visual anchor is the order status badge column; "View Details" links are secondary and provide drill-down access.

- `table table-hover` with columns: Order ID (truncated to 8 chars + ellipsis), Date, Status (badge), Total, Actions
- Actions: "View Details" `btn btn-outline-primary btn-sm`
- Empty state: `text-muted` centered message

### Order Detail (`/orders/{id}`)
**Focal point:** The primary visual anchor is the order status badge in the header; conditional action buttons (Cancel / Return) are secondary focal points rendered only when eligible.

- Order summary header: order ID, date, status badge
- `table` of line items: product name, size/color, qty, price
- Total `fw-bold`
- Conditional buttons (rendered by template logic per status eligibility):
  - Cancel: `btn btn-danger` — shown when status is `placed` or `paid`
  - Request Return: `btn btn-warning` — shown when status is `paid`, `processing`, or `shipped`

### Order Confirmation (`/orders/{id}/confirmation`)
**Focal point:** The primary visual anchor is the order confirmation card with the order ID and status badge; "View All Orders" is the single forward-navigation CTA.

- `card` centered: order ID, items list, total, payment method, status badge, "View All Orders" `btn btn-primary` link

### Auth Pages (login, register, reset-request, reset-confirm)
**Focal point:** The primary visual anchor on every auth page is the full-width primary submit button (`btn btn-primary w-100`) — all form fields are prereqs that lead to it.

- Centered narrow `col-md-4 offset-md-4` card layout
- `form` with `mb-3` field groups, `label`, `form-control` inputs
- Primary submit: `btn btn-primary w-100`
- Login page: `small text-muted` demo hint below form — "Demo: alice@example.com / password123"
- Error display: Bootstrap `alert alert-danger` above form

Source: D-05, D-06, D-07, D-08, D-09, D-11, D-12, specifics section (CONTEXT.md)

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA — add to cart | "Add to Cart" |
| Primary CTA — checkout | "Place Order" |
| Primary CTA — login | "Sign In" |
| Primary CTA — register | "Create Account" |
| Primary CTA — password reset request | "Send Reset Link" |
| Primary CTA — password reset confirm | "Set New Password" |
| Cart empty state heading | "Your cart is empty" |
| Cart empty state body | "Browse our shoes and add items to get started." + "Browse Products" link |
| Orders empty state heading | "No orders yet" |
| Orders empty state body | "Place your first order to see it here." |
| Checkout payment failure | "Payment failed. Please try a different payment method or contact support." |
| Checkout out-of-stock failure | "One or more items in your cart are out of stock. Please update your cart and try again." |
| Order cancel confirmation | "Cancel Order: Are you sure you want to cancel this order? This cannot be undone." |
| Order return confirmation | "Request Return: Submit a return request for this order?" |
| Auth — login error | "Invalid email or password. Please try again." |
| Auth — register error (duplicate) | "An account with this email already exists. Sign in instead?" |
| Password reset — success | "Check the API response for your reset token. Use it to set a new password." |
| Password reset — token invalid | "This reset link is invalid or has expired. Request a new one." |
| Demo credential hint | "Demo accounts: alice@example.com or bob@example.com — password: password123" |

Destructive actions in this phase:
- Cancel Order — confirmation via inline `alert alert-warning` with confirm button (not a modal; keep it simple)
- No JS modal required — POST form with a `?confirm=true` query param approach, or a second confirmation page is acceptable

---

## Interaction Contracts

### Flash Messages
- Set via Jinja2 session (itsdangerous `SessionMiddleware` already in pyproject.toml)
- Categories: `success` → `alert-success`, `error` → `alert-danger`, `info` → `alert-info`, `warning` → `alert-warning`
- Rendered in `base.html` above page content, auto-dismissible (Bootstrap JS dismiss button)
- One flash per redirect — not stacked

### Form Validation
- Server-side only (no JS validation in Phase 3)
- Validation errors: re-render the form with an `alert alert-danger` at the top listing the error(s)
- Field-level inline errors: `is-invalid` class on `form-control` + `invalid-feedback` div (optional, only where it adds clarity)

### Redirect After POST
- All successful POST actions redirect (PRG pattern) to avoid double-submit on browser back
- Add to Cart → redirect to `/cart`
- Checkout success → redirect to `/orders/{id}/confirmation`
- Login success → redirect to `?next=` param or `/products`
- Logout → redirect to `/login`

### Unauthenticated Access
- Protected routes redirect to `/login?next={original_url}`
- After login, redirect to `next` param (validated to be a relative path — no open redirect)

### Category Filter + Search Combination
- Category tabs render as `<a href="/products?category=Running">` links
- Search form submits to `GET /products?q={term}&category={active}`
- Active category preserved in the search form's hidden input

### Quantity Input (Product Detail)
- Default value: 1
- `min="1"` attribute — browser enforces no zero/negative
- `max` attribute set to current inventory count from template context

### Variant Selector (Product Detail)
- Two `<select>` elements: Size and Color
- Populated from `product.variants` in template context
- Both required — form validation prevents submission without selection

---

## Registry Safety

Not applicable — no shadcn, no third-party component registry. Bootstrap 5 is loaded via official CDN only.

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| Bootstrap CDN (cdn.jsdelivr.net) | Bootstrap 5.3 CSS + JS bundle | not required — official CDN |
| Bootstrap Icons CDN | bootstrap-icons 1.11 CSS | not required — official CDN |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
