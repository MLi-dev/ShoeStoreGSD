---
phase: 03-web-ui-rest-api
plan: 04
type: execute
wave: 3
depends_on:
  - "03-03"
files_modified:
  - app/api/catalog_router.py
  - app/web/templates/products/list.html
  - app/web/templates/products/detail.html
autonomous: true
requirements:
  - CAT-02
  - CAT-03

must_haves:
  truths:
    - "GET /products renders product list with search box and category filter tabs"
    - "GET /products?q=runner returns only products matching 'runner' (case-insensitive)"
    - "GET /products?category=hiking returns only hiking products"
    - "GET /products?q=trail&category=running returns trail products within running category"
    - "GET /products/{id} renders product detail with variant dropdowns and Add to Cart form"
    - "GET /products/{id} for unknown ID returns 404"
    - "Add to Cart form includes hidden product_id input"
  artifacts:
    - path: "app/api/catalog_router.py"
      provides: "GET /products, GET /products/{id}, POST /cart/add"
      contains: "catalog_service.search_products"
    - path: "app/web/templates/products/list.html"
      provides: "Product grid with card components, search input, category nav tabs"
      contains: "nav-tabs"
    - path: "app/web/templates/products/detail.html"
      provides: "Product detail with variant selects and Add to Cart POST form"
      contains: "type=\"hidden\" name=\"product_id\""
  key_links:
    - from: "app/api/catalog_router.py"
      to: "app.lib.catalog.catalog_service.search_products"
      via: "results = catalog_service.search_products(q=q, category=category)"
      pattern: "search_products"
    - from: "app/web/templates/products/detail.html"
      to: "POST /cart/add"
      via: "form action='/cart/add' method='post'"
      pattern: "action=\"/cart/add\""
    - from: "app/web/templates/products/list.html"
      to: "GET /products?category="
      via: "nav-tab links with category param"
      pattern: "\\?category="
---

<objective>
Build the catalog router and product templates (list + detail). This delivers the core shopping browse experience: users can search for shoes, filter by category, and view a product detail page with a variant selector before adding to cart.

Purpose: Product browsing is the entry point for the purchase flow. The catalog pages are the first thing a demo viewer will see.

Output: catalog_router.py (replacing the stub), products/list.html, products/detail.html. POST /cart/add is also defined here (delegates to cart_service — the actual cart_router.py in Plan 05 handles GET /cart and other cart mutations).
</objective>

<execution_context>
@/Users/matthew/Dev/ShoeStoreGSD/.planning/phases/03-web-ui-rest-api/03-UI-SPEC.md
@/Users/matthew/Dev/ShoeStoreGSD/.planning/phases/03-web-ui-rest-api/03-RESEARCH.md
@/Users/matthew/Dev/ShoeStoreGSD/.planning/phases/03-web-ui-rest-api/03-PATTERNS.md
</execution_context>

<context>
@/Users/matthew/Dev/ShoeStoreGSD/.planning/ROADMAP.md
@/Users/matthew/Dev/ShoeStoreGSD/app/lib/catalog/catalog_service.py
@/Users/matthew/Dev/ShoeStoreGSD/app/lib/catalog/models.py
@/Users/matthew/Dev/ShoeStoreGSD/app/lib/cart/cart_service.py
@/Users/matthew/Dev/ShoeStoreGSD/app/lib/auth/dependencies.py

<interfaces>
<!-- Extracted interfaces -->

From app/lib/catalog/catalog_service.py:
```python
def search_products(q: str = "", category: str = "") -> list[Product]
def get_product(product_id: str) -> Product | None
```

From app/lib/catalog/models.py:
```python
@dataclass
class Variant:
    size: str | None = None
    color: str | None = None

@dataclass
class Product:
    id: str; name: str; description: str; unit_price: float
    inventory: int; category: str
    image_url: str | None = None
    variants: list[Variant] = field(default_factory=list)
```

From app/lib/cart/cart_service.py:
```python
async def add_item(user_id: str, product_id: str, quantity: int) -> dict
# Returns: {"success": True, "data": {"cart": ...}}
#       or {"success": False, "code": "PRODUCT_NOT_FOUND"|"OUT_OF_STOCK", ...}
```

From app/lib/auth/dependencies.py:
```python
async def get_current_user_web(request: Request, access_token: ...) -> User
# Raises HTTPException(307) → /login?next=... when unauthenticated
```

From 03-UI-SPEC.md (Product List component):
- Search: `input-group` with text input and "Search" btn-primary
- Category tabs: `nav nav-tabs` All/Running/Hiking/Slides/Sandals/Socks
- Product grid: `row row-cols-1 row-cols-md-3 g-4` of `card h-100`
- Each card: bg-secondary placeholder div 200px, card-title, card-text small (category), price fw-semibold, "View Details" btn-primary btn-sm stretched-link
- NO inline Add to Cart on list page

From 03-UI-SPEC.md (Product Detail component):
- row with col-md-6 left (placeholder image) and col-md-6 right (info + form)
- h2 name, text-muted category, fs-3 fw-semibold price, small text-muted inventory
- select.form-select for Size, select.form-select for Color
- input[type=number] min=1, max=inventory, value=1 for quantity
- "Add to Cart" btn btn-primary btn-lg w-100 — POST form to /cart/add
- CRITICAL: hidden input name="product_id" value="{{ product.id }}" (RESEARCH.md Pitfall 7)

From 03-UI-SPEC.md (copywriting):
- "View Details" on product cards
- "Add to Cart" on detail page
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build catalog_router.py</name>
  <files>
    app/api/catalog_router.py
  </files>
  <read_first>
    - app/api/catalog_router.py (current stub — will be replaced entirely)
    - app/lib/catalog/catalog_service.py (search_products, get_product signatures)
    - app/lib/cart/cart_service.py (add_item signature and return shape)
    - app/lib/auth/dependencies.py (get_current_user_web)
    - app/lib/auth/models.py (User dataclass)
    - .planning/phases/03-web-ui-rest-api/03-RESEARCH.md (Pattern 3: TemplateResponse keyword-arg form)
  </read_first>
  <action>
Replace the stub catalog_router.py with the full implementation. This router handles:
- GET /products — product list with optional q and category query params (CAT-02, D-10, D-11)
- GET /products/{product_id} — product detail page (CAT-03, D-12)
- POST /cart/add — add item to cart, redirect to /cart (D-12, PRG)

NOTE: POST /cart/add lives here because Add to Cart is a form action on the product detail page. The cart router (Plan 05) handles GET /cart, POST /cart/update, POST /cart/remove, and POST /checkout.

The categories list must match the seeded categories exactly: `["running", "hiking", "slides", "sandals", "socks"]`.

```python
# app/api/catalog_router.py
# Catalog router: product list (search + filter) and product detail.
# Also handles POST /cart/add (Add to Cart form on product detail page).
# Routes: GET /products, GET /products/{product_id}, POST /cart/add
from fastapi import APIRouter, Depends, Form, HTTPException, Request
from fastapi.responses import HTMLResponse, RedirectResponse
from fastapi.templating import Jinja2Templates

from app.lib.auth.dependencies import get_current_user_web
from app.lib.auth.models import User
from app.lib.cart import cart_service
from app.lib.catalog import catalog_service

router = APIRouter(tags=["catalog"])
templates = Jinja2Templates(directory="app/web/templates")

CATEGORIES = ["running", "hiking", "slides", "sandals", "socks"]


@router.get("/products", response_class=HTMLResponse)
async def product_list(request: Request, q: str = "", category: str = ""):
    """Product list page with keyword search and category filter tabs (CAT-02, D-10, D-11).

    Does not require authentication — browsing is public.
    """
    products = catalog_service.search_products(q=q, category=category)
    return templates.TemplateResponse(
        request=request,
        name="products/list.html",
        context={
            "products": products,
            "q": q,
            "active_category": category,
            "categories": CATEGORIES,
        },
    )


@router.get("/products/{product_id}", response_class=HTMLResponse)
async def product_detail(request: Request, product_id: str):
    """Product detail page with variant selector and Add to Cart form (CAT-03, D-12).

    Does not require authentication — viewing product details is public.
    """
    product = catalog_service.get_product(product_id)
    if not product:
        raise HTTPException(status_code=404, detail="Product not found")
    return templates.TemplateResponse(
        request=request,
        name="products/detail.html",
        context={"product": product},
    )


@router.post("/cart/add")
async def add_to_cart(
    request: Request,
    product_id: str = Form(...),
    size: str = Form(...),
    color: str = Form(...),
    quantity: int = Form(1),
    current_user: User = Depends(get_current_user_web),
):
    """Add a product variant to the authenticated user's cart (D-12, PRG pattern).

    On success: redirect to /cart (303).
    On failure (out of stock, product not found): flash error + redirect to product page.
    """
    result = await cart_service.add_item(
        user_id=current_user.id,
        product_id=product_id,
        quantity=quantity,
    )
    if not result["success"]:
        request.session["flash"] = {"category": "danger", "message": result["message"]}
        return RedirectResponse(url=f"/products/{product_id}", status_code=303)
    return RedirectResponse(url="/cart", status_code=303)
```
  </action>
  <verify>
    <automated>uv run python -c "from app.api.catalog_router import router; print('catalog_router OK')"</automated>
  </verify>
  <acceptance_criteria>
    - app/api/catalog_router.py contains `def product_list`, `def product_detail`, `def add_to_cart`
    - app/api/catalog_router.py contains `catalog_service.search_products`
    - app/api/catalog_router.py contains `CATEGORIES = ["running", "hiking", "slides", "sandals", "socks"]`
    - POST /cart/add uses `Depends(get_current_user_web)` (auth required for cart mutations)
    - GET /products and GET /products/{id} do NOT use Depends (public browsing)
    - `grep "Optional\[" app/api/catalog_router.py` returns nothing
    - `uv run python -c "from app.api.catalog_router import router; print('catalog_router OK')"` exits 0
  </acceptance_criteria>
  <done>catalog_router.py replaces the stub, all three routes are defined, and the module imports without error.</done>
</task>

<task type="auto">
  <name>Task 2: Build products/list.html and products/detail.html templates</name>
  <files>
    app/web/templates/products/list.html
    app/web/templates/products/detail.html
  </files>
  <read_first>
    - .planning/phases/03-web-ui-rest-api/03-UI-SPEC.md (Product List and Product Detail component specs, copywriting, color rules)
    - .planning/phases/03-web-ui-rest-api/03-RESEARCH.md (Pitfall 7: hidden product_id input; Pitfall 5: form field names must match)
    - app/web/templates/base.html (block structure to extend)
  </read_first>
  <action>
**app/web/templates/products/list.html** (per UI-SPEC Product List section, D-11):

Template receives context: `products` (list[Product]), `q` (str), `active_category` (str), `categories` (list[str]).

Key constraints per UI-SPEC:
- Search: `input-group` with `name="q"` text input and "Search" `btn-primary`; active category preserved as hidden input `name="category"` so search+filter combine (D-11)
- Category tabs: `nav nav-tabs` links to `/products?category={cat}` — lowercase category names from CATEGORIES list; "All" tab goes to `/products`
- Active tab: `class="nav-link active"` when `active_category == cat` (or empty for All)
- Product grid: `row row-cols-1 row-cols-md-3 g-4`; each item `col`
- Card: `card h-100`; placeholder image `div` with `bg-secondary` style `height:200px`; `card-body`; `card-title` (product.name); `card-text small text-muted` (product.category); price as `fw-semibold` text; "View Details" as `btn btn-primary btn-sm stretched-link` href to `/products/{product.id}`
- NO inline Add to Cart on list page

```html
{% extends "base.html" %}
{% block title %}Products — ShoeStore{% endblock %}
{% block content %}

<h2 class="mb-4">Shoes</h2>

<!-- Search bar -->
<form method="get" action="/products" class="mb-4">
  <input type="hidden" name="category" value="{{ active_category }}">
  <div class="input-group">
    <input type="text" class="form-control" name="q" value="{{ q }}"
           placeholder="Search shoes...">
    <button type="submit" class="btn btn-primary">Search</button>
  </div>
</form>

<!-- Category filter tabs -->
<ul class="nav nav-tabs mb-4">
  <li class="nav-item">
    <a class="nav-link {% if not active_category %}active{% endif %}"
       href="/products{% if q %}?q={{ q }}{% endif %}">All</a>
  </li>
  {% for cat in categories %}
  <li class="nav-item">
    <a class="nav-link {% if active_category == cat %}active{% endif %}"
       href="/products?category={{ cat }}{% if q %}&q={{ q }}{% endif %}">
      {{ cat | capitalize }}
    </a>
  </li>
  {% endfor %}
</ul>

<!-- Product grid -->
{% if products %}
<div class="row row-cols-1 row-cols-md-3 g-4">
  {% for product in products %}
  <div class="col">
    <div class="card h-100">
      <div class="bg-secondary" style="height:200px;"></div>
      <div class="card-body">
        <h5 class="card-title">{{ product.name }}</h5>
        <p class="card-text small text-muted">{{ product.category | capitalize }}</p>
        <p class="card-text fw-semibold">${{ "%.2f" | format(product.unit_price) }}</p>
        <a href="/products/{{ product.id }}"
           class="btn btn-primary btn-sm stretched-link">View Details</a>
      </div>
    </div>
  </div>
  {% endfor %}
</div>
{% else %}
<div class="text-center py-5">
  <p class="text-muted">No products found matching your search.</p>
  <a href="/products" class="btn btn-outline-primary">Clear filters</a>
</div>
{% endif %}

{% endblock %}
```

**app/web/templates/products/detail.html** (per UI-SPEC Product Detail section, D-12):

Template receives context: `product` (Product dataclass with .id, .name, .description, .unit_price, .inventory, .category, .variants list[Variant]).

Key constraints per UI-SPEC and RESEARCH.md:
- Two-column layout: col-md-6 placeholder image, col-md-6 info+form
- h2 name; text-muted category; fs-3 fw-semibold price; small text-muted inventory count
- Two `select.form-select` elements: one for size, one for color — populated from product.variants
- Quantity `input[type=number]` min=1 max=product.inventory value=1
- CRITICAL: hidden input `name="product_id"` (Pitfall 7 — product_id is not in the POST body otherwise)
- Form POSTs to `/cart/add` with method="post"
- "Add to Cart" `btn btn-primary btn-lg w-100`
- form field names: `product_id`, `size`, `color`, `quantity` (must match cart_router Form() params exactly — Pitfall 5)

Size and color options: extract unique values from product.variants. If variants list is empty, show a default option.

```html
{% extends "base.html" %}
{% block title %}{{ product.name }} — ShoeStore{% endblock %}
{% block content %}

<div class="row">
  <!-- Product image placeholder -->
  <div class="col-md-6">
    <div class="bg-secondary rounded" style="height:400px;"></div>
  </div>

  <!-- Product info + Add to Cart form -->
  <div class="col-md-6">
    <h2>{{ product.name }}</h2>
    <p class="text-muted">{{ product.category | capitalize }}</p>
    <p class="fs-3 fw-semibold">${{ "%.2f" | format(product.unit_price) }}</p>
    <p class="small text-muted">
      {% if product.inventory > 0 %}
        {{ product.inventory }} in stock
      {% else %}
        <span class="text-danger">Out of stock</span>
      {% endif %}
    </p>
    <p>{{ product.description }}</p>

    <form method="post" action="/cart/add">
      <!-- CRITICAL: product_id must be a hidden POST field (RESEARCH.md Pitfall 7) -->
      <input type="hidden" name="product_id" value="{{ product.id }}">

      <div class="mb-3">
        <label for="size" class="form-label">Size</label>
        <select class="form-select" id="size" name="size" required>
          {% set sizes = product.variants | map(attribute='size') | select | unique | list %}
          {% if sizes %}
            {% for s in sizes %}
            <option value="{{ s }}">{{ s }}</option>
            {% endfor %}
          {% else %}
            <option value="One Size">One Size</option>
          {% endif %}
        </select>
      </div>

      <div class="mb-3">
        <label for="color" class="form-label">Color</label>
        <select class="form-select" id="color" name="color" required>
          {% set colors = product.variants | map(attribute='color') | select | unique | list %}
          {% if colors %}
            {% for c in colors %}
            <option value="{{ c }}">{{ c }}</option>
            {% endfor %}
          {% else %}
            <option value="Default">Default</option>
          {% endif %}
        </select>
      </div>

      <div class="mb-3">
        <label for="quantity" class="form-label">Quantity</label>
        <input type="number" class="form-control" id="quantity" name="quantity"
               min="1" max="{{ product.inventory }}" value="1" required>
      </div>

      <button type="submit" class="btn btn-primary btn-lg w-100"
              {% if product.inventory == 0 %}disabled{% endif %}>
        Add to Cart
      </button>
    </form>

    <div class="mt-3">
      <a href="/products" class="text-muted small">&larr; Back to Products</a>
    </div>
  </div>
</div>

{% endblock %}
```
  </action>
  <verify>
    <automated>uv run pytest tests/ -x -q -k "not xfail" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - app/web/templates/products/list.html exists and contains `nav-tabs`, `row-cols-md-3`, `View Details`
    - app/web/templates/products/detail.html exists and contains `type="hidden" name="product_id"` and `name="size"` and `name="color"` and `name="quantity"`
    - detail.html form action is `/cart/add` with method `post`
    - list.html search form preserves active_category with `<input type="hidden" name="category"`
    - `uv run python -c "from main import app; print('OK')"` exits 0 (no template import errors)
    - No prior passing tests are broken: `uv run pytest tests/unit/ -x -q` passes
  </acceptance_criteria>
  <done>Both product templates exist with correct Bootstrap structure, form fields matching router params, and hidden product_id input.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser GET /products?q= | q and category are attacker-controlled; reflected into templates |
| Browser POST /cart/add | product_id, size, color, quantity from untrusted form body |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02 | Tampering | XSS via {{ q }} in products/list.html | mitigate | Jinja2 auto-escaping is on for .html templates; `{{ q }}` in input value attribute is escaped automatically |
| T-03-05 | Elevation of Privilege | POST /cart/add cross-user cart pollution | mitigate | user_id for add_item() is resolved from JWT cookie via Depends(get_current_user_web) — never from form body; attacker cannot inject another user's ID |
| T-03-06 | Tampering | Double-submit on Add to Cart | mitigate | POST /cart/add returns 303 redirect on both success and failure — PRG pattern prevents replay |
| T-03-03 | Tampering | CSRF on POST /cart/add | accept | SameSite=Lax on the auth cookie prevents cross-site form POSTs from carrying the cookie; baseline protection per ASVS L1 |
</threat_model>

<verification>
After both tasks complete, run the full unit suite and verify the app starts:

```
uv run pytest tests/unit/ -x -q
uv run python -c "from main import app; print('app OK')"
```
</verification>

<success_criteria>
- catalog_router.py defines GET /products, GET /products/{product_id}, POST /cart/add
- products/list.html has category tabs, search form, and product card grid
- products/detail.html has variant selects, quantity input, hidden product_id, and Add to Cart form posting to /cart/add
- All unit tests still pass
- The app imports cleanly from main.py
</success_criteria>

<output>
After completion, create `.planning/phases/03-web-ui-rest-api/03-04-SUMMARY.md` with: files created, key decisions (public browsing, where POST /cart/add lives), test results.
</output>
